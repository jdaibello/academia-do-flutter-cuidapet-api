name: cuidapet_api
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
    sdk: ">=2.15.0 <3.0.0"

dependencies:
    args: ^2.0.0
    shelf: ^1.1.0
    shelf_router: ^1.0.0
    dotenv: ^3.0.0

dev_dependencies:
    lints: ^1.0.0
